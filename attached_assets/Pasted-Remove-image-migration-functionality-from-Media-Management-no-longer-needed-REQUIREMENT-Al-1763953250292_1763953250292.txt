Remove image migration functionality from Media Management - no longer needed:

REQUIREMENT:

All images are now successfully uploaded to Cloudinary. Remove the migration button and related migration functionality from the app since it's no longer needed.

REMOVE THE FOLLOWING:

1. REMOVE MIGRATION UI COMPONENTS:
   
   In Admin → Settings → Image Storage Migration card:
   
   Remove:
   - "Migrate Images to Cloudinary" button
   - Migration progress indicators
   - Migration status messages
   - Migration results/report display
   - Any migration-related UI elements

2. REMOVE MIGRATION BACKEND ENDPOINT:
   
   Delete the migration endpoint:
   - POST /api/owner/migrate-images-to-cloudinary
   - Remove entire function/handler
   - Remove any migration helper functions

3. REMOVE MIGRATION SCRIPT FILES:
   
   Delete any migration script files created:
   - /scripts/migrate-images.js (if exists)
   - /utils/image-migration.js (if exists)
   - Any other migration-related files

4. CLEAN UP MEDIA MANAGEMENT CARD:
   
   The Media Management card should now only show:
   
   ✅ Storage Status:
   - Current Storage: Cloudinary
   - Environment: easybuydelivery-dev (or prod)
   - Status: Active
   
   ✅ Storage Info (optional):
   - Cloud Name: [your-cloud-name]
   - Folder Structure: easybuydelivery-{env}/
   - Connection Status: Connected ✓
   
   ❌ Remove:
   - Migration button
   - Migration status
   - Migration logs
   - Cleanup button (if added)

5. KEEP CLOUDINARY FUNCTIONALITY:
   
   Keep all working Cloudinary features:
   - Logo upload to Cloudinary ✓
   - Merchant photo upload to Cloudinary ✓
   - Menu item photo upload to Cloudinary ✓
   - Image display from Cloudinary URLs ✓
   - Cloudinary configuration ✓

6. REMOVE MIGRATION-RELATED CODE:
   
   Delete:
   - Migration route handlers
   - Migration service functions
   - Migration status tracking
   - Migration error handling (specific to migration)
   - Any database tables/fields for migration tracking

7. CLEAN UP IMPORTS:
   
   Remove unused imports related to migration:
   - File system operations for scanning /uploads
   - Progress tracking libraries
   - Migration utility functions

8. UPDATE COMMENTS/DOCUMENTATION:
   
   Remove any code comments mentioning:
   - "TODO: Run migration"
   - "Migration script"
   - Instructions about migration

The app now uses Cloudinary for all image uploads. The migration functionality was a one-time operation and is no longer needed. Clean up all migration-related code to simplify the codebase.